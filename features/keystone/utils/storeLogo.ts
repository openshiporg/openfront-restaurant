// Keep the Keystone import path stable while sharing the pure sanitizer with
// the Store Settings client preview. The model remains the enforcement boundary.
export { sanitizeStoreLogoSvg } from "../../lib/sanitize-store-logo";
